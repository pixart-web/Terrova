services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-terrova}
      POSTGRES_USER: ${POSTGRES_USER:-terrova}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-terrova}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - terrova_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-terrova} -d ${POSTGRES_DB:-terrova}']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  terrova_postgres_data:
